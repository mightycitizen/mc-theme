import * as rollup from 'rollup';

declare function twig(): {
    name: string;
    transform(this: rollup.TransformPluginContext, code: string, id: string): string | null;
};

export { twig as default };
